# Moved

This repository has moved to **[`GuyErreich/ansible-collections`](https://github.com/GuyErreich/ansible-collections)**.

Install the callback from Ansible Galaxy:

```bash
ansible-galaxy collection install guyerreich.outputs
```

Or from the monorepo path:

```bash
ansible-galaxy collection install \
  git+https://github.com/GuyErreich/ansible-collections.git,main#/collections/ansible_collections/guyerreich/outputs
```

This repository is archived and no longer maintained.
